<%= form_for([product, bid]) do |f| %>
  <% if bid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bid.errors.count, "error") %> prohibited this bid from being saved:</h2>

      <ul>
      <% bid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :value, "Precio" %>
    <%= f.number_field :value %>
  </div>

  <div class="form-group">
    <%= f.label :bidder_name, "Quién" %>
    <%= f.text_field :bidder_name %>
  </div>

  <div class="form-group">
    <%= f.submit "Nueva Puja", class: "btn btn-primary" %>
  </div>
<% end %>
